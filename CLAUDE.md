# SRC Review Skill

After every update to this skill (SKILL.md, phase-c-workflow.js, scripts/*, CLAUDE.md), commit and push to GitHub:

```bash
cd ~/.claude/skills/src-review
git add -A
git commit -m "update: <brief description>"
git push origin main
```

Git credentials are managed by `gh auth`.
